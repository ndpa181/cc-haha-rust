[package]
name = "verification"
version = "0.1.0"
edition = "2021"
description = "Adversarial verification framework"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]

[lib]
path = "src/lib.rs"
